# Run with `flutter pub run ffigen --config ffigen.yaml`.
name: Libxml2FlutterPluginBindings
description: |
  Bindings for `src/libxml2_flutter_plugin.h`.

  Regenerate bindings with `flutter pub run ffigen --config ffigen.yaml`.
output: 'lib/libxml2_flutter_plugin_bindings_generated.dart'
headers:
  entry-points:
    - 'src/libxml2_flutter_plugin.h'
  include-directives:
    - 'src/libxml2_flutter_plugin.h'
preamble: |
  // ignore_for_file: always_specify_types
  // ignore_for_file: camel_case_types
  // ignore_for_file: non_constant_identifier_names
comments:
  style: any
  length: full
